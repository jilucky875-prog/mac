name: macOS Native VNC (Golden Master - Fixed Final)

on: workflow_dispatch

concurrency:
  group: "mac-native-vnc"
  cancel-in-progress: false

jobs:
  macos-vnc-pro:
    runs-on: macos-14
    timeout-minutes: 360

    steps:
      - name: 🔔 Notify Start
        run: |
          curl -sS -X POST -H "Content-Type: application/json" \
            -d "{\"message\":\"🍎 *Mac Starting...*\nMode: Final Working RDP\"}" \
            "${{ secrets.N8N_WEBHOOK }}" || true

      - name: ⚙️ System Prep & Wake Screen
        run: |
          sudo mdutil -i off -a || true
          sudo pmset -a displaysleep 0 disksleep 0 sleep 0 || true
          caffeinate -u -t 10 &
          
          cat << 'EOF' > ram_monitor.sh
          #!/bin/bash
          while true; do
            FREE_PAGES=$(vm_stat | grep "Pages free" | awk '{print $3}' | sed 's/\.//')
            if [ "$FREE_PAGES" -lt 200000 ]; then sudo purge || true; fi
            sleep 30
          done
          EOF
          chmod +x ram_monitor.sh
          ./ram_monitor.sh &

      - name: 🔐 Setup VNC (Dual Daemon Rebuild + Secure Pass)
        env:
          VNC_PASS: ${{ secrets.VNC_PASSWORD }}
        run: |
          if [ -z "$VNC_PASS" ]; then
            echo "ERROR: VNC_PASSWORD secret missing"
            exit 1
          fi

          echo "1) Stop and kill stuck processes..."
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -stop || true
          sudo pkill screensharingd || true
          sudo pkill ARDAgent || true

          echo "2) Resetting Daemons (Screen Sharing + Remote Management)..."
          sudo launchctl bootout system /System/Library/LaunchDaemons/com.apple.screensharing.plist || true
          sudo launchctl bootout system /System/Library/LaunchDaemons/com.apple.RemoteManagement.launchd || true
          sleep 3
          sudo launchctl bootstrap system /System/Library/LaunchDaemons/com.apple.screensharing.plist || true
          sudo launchctl bootstrap system /System/Library/LaunchDaemons/com.apple.RemoteManagement.launchd || true

          echo "3) Final Configuration (Single Line Safe Execution)..."
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -activate -configure -allowAccessFor -allUsers -configure -access -on -privs -all -configure -clientopts -setreqperm -reqperm no -setvnclegacy -vnclegacy yes -setvncpw -vncpw "$VNC_PASS" -restart -agent

          echo "4) Final Verification..."
          osascript -e 'tell application "System Events" to key code 123' || true
          sleep 5
          sudo lsof -nP -iTCP:5900 -sTCP:LISTEN || netstat -an | grep 5900 || echo "WARN: 5900 not listening"

      - name: 💾 Setup GDrive Sync
        env:
          RCLONE_CONFIG_DATA: ${{ secrets.RCLONE_CONFIG }}
        run: |
          brew install rclone
          mkdir -p ~/.config/rclone
          echo "$RCLONE_CONFIG_DATA" > ~/.config/rclone/rclone.conf
          mkdir -p ~/Desktop/MyWorkspace
          rclone mkdir gdrive:RDP_Backup || true
          rclone copy gdrive:RDP_Backup ~/Desktop/MyWorkspace || true

      - name: 🔌 Start Pinggy TCP Tunnel
        run: |
          echo "Starting Pinggy Tunnel..."
          TUNNEL_DEST="tcp@a.pinggy.io"
          # Canonical Pinggy syntax: -R0:localhost:5900
          ssh -p 443 -R0:localhost:5900 -o StrictHostKeyChecking=no -o ServerAliveInterval=30 "$TUNNEL_DEST" > pinggy.log 2>&1 &
          
          sleep 12
          URL=$(grep -o "tcp://[0-9a-zA-Z.-]*:[0-9]*" pinggy.log | head -n 1)
          if [ -z "$URL" ]; then
            echo "ERROR: Pinggy Tunnel failed"
            cat pinggy.log
            exit 1
          fi
          CLEAN_URL="${URL#tcp://}"
          
          curl -sS -X POST -H "Content-Type: application/json" \
            -d "{\"message\":\"🍎 *Mac Ready!*\n🖥️ VNC Address: \`$CLEAN_URL\`\n👤 User: (Leave Blank)\n🔑 Pass: (Use GitHub Secret)\"}" \
            "${{ secrets.N8N_WEBHOOK }}" || true

      - name: 🔄 Keep-Alive & Auto-Backup
        run: |
          cleanup() {
            rclone sync ~/Desktop/MyWorkspace gdrive:RDP_Backup || true
            exit 0
          }
          trap 'cleanup' SIGINT SIGTERM EXIT
          START=$(date +%s)
          while true; do
            NOW=$(date +%s); DIFF=$((NOW - START))
            if [ $DIFF -ge 21000 ]; then exit 0; fi
            if [ $((DIFF % 300)) -lt 60 ] && [ $DIFF -gt 100 ]; then
              rclone sync ~/Desktop/MyWorkspace gdrive:RDP_Backup || true
            fi
            sleep 60
          done
